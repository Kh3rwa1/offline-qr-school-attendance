apiVersion: batch/v1
kind: Job
metadata:
  name: school-attendance-migration
spec:
  backoffLimit: 3
  template:
    metadata:
      name: school-attendance-migration
    spec:
      serviceAccountName: school-attendance-migration-sa
      restartPolicy: Never
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: migrate
          image: ghcr.io/kh3rwa1/offline-qr-school-attendance@sha256:e0f436e2cc8bbf0342c241bbfa4a281f6b2894380123456789abcdef01234567
          imagePullPolicy: IfNotPresent
          command: ["node", "dist/migrate.cjs"]
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp-volume
              mountPath: /tmp
          envFrom:
            - secretRef:
                name: school-attendance-secrets
            - configMapRef:
                name: school-attendance-config
      volumes:
        - name: tmp-volume
          emptyDir: {}
