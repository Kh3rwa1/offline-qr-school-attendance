name: DESFire Cryptographic Simulation & APDU Verification Gate

on:
  workflow_dispatch:
    inputs:
      enforce_live_hardware:
        description: 'Enforce physical reader and fail closed if absent (true/false)'
        required: true
        default: 'false'
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  physical-hardware-gate:
    name: DESFire EV2/EV3 PC/SC Hardware & APDU Verification Gate
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout Code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup Node.js Environment
        uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0
        with:
          node-version: 22

      - name: Install Dependencies
        run: npm ci

      - name: Verify PC/SC Smartcard Subsystem Daemon
        run: |
          if command -v systemctl &> /dev/null; then
            systemctl is-active --quiet pcscd || sudo systemctl start pcscd || true
          fi

      - name: Annotate Simulation Mode
        run: echo "::notice::This gate verifies cryptographic correctness in simulation. Physical certification requires on-site hardware commissioning per SCHOOL_SITE_RFID_CERTIFICATION_PROTOCOL.md"

      - name: Execute Hardware Verification & APDU Benchmarks
        env:
          HARDWARE_RELEASE_GATE: ${{ inputs.enforce_live_hardware == 'true' && '1' || '0' }}
          GITHUB_SHA: ${{ github.sha }}
        run: npx tsx scripts/hardware-runner.ts

      - name: Upload Hardware Certification Reports
        if: always()
        uses: actions/upload-artifact@b4b15b8c7c6ac21ea08fcf65892d2ee8f75cf882 # v4.4.3
        with:
          name: hardware-certification-reports
          path: |
            output/hardware-certification-report.json
            output/hardware-certification-report.md
          if-no-files-found: ignore
