apiVersion: v1
kind: ConfigMap
metadata:
  name: school-attendance-config
data:
  NODE_ENV: "production"
  PORT: "3000"
  WEB_REPLICA_COUNT: "2"
  SMS_WORKER_REPLICA_COUNT: "2"
  PG_POOL_MAX_APP: "15"
  PG_POOL_MAX_SYS: "5"
  MAX_ALLOWED_DB_CONNECTIONS: "100"
  PG_STATEMENT_TIMEOUT_MS: "10000"
